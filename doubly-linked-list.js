// Do not change this
class DoublyLinkedListNode {
  constructor(val) {
    this.value = val;
    this.prev = null;
    this.next = null;
  }
}

class DoublyLinkedList {
  constructor() {
    // Fill this in
  }

  addToHead(val) { // O(1)
    // Fill this in
  }

  addToTail(val) { // O(1)
    // Fill this in
  }
}

module.exports = DoublyLinkedList;